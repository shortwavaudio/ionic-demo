import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.shortwavaudio.ionic-demo',
  appName: 'ionic-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
